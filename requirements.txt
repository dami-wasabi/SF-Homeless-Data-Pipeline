# requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Runtime dependencies
# boto3 is available in every Lambda runtime; pin here for local dev parity.
boto3==1.34.69
botocore==1.34.69

# Development / CI only – not deployed to Lambda
pytest==8.1.1
ruff==0.3.4
mypy==1.9.0
boto3-stubs[dynamodb,s3,ssm,events]==1.34.69
